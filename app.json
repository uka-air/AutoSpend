{
  "expo": {
    "name": "AutoSpend",
    "slug": "autospend-mobile",
    "version": "2.3.1",
    "orientation": "portrait",
    "ios": {
      "supportsTablet": true,
      "infoPlist": {
        "NSCameraUsageDescription": "AutoSpend uses the camera to capture receipts for OCR.",
        "NSPhotoLibraryUsageDescription": "AutoSpend uses your photo library to import receipt images."
      }
    },
    "android": {
      "permissions": [
        "CAMERA",
        "READ_MEDIA_IMAGES"
      ]
    }
  }
}
